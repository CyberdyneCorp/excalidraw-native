# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved

# Xcode
# NOTE: ExcalidrawSwift.xcodeproj IS committed (generated from project.yml but
# kept in the repo so it's stable and never drifts). Only ignore the
# user-specific / cache contents inside any .xcodeproj.
*.xcworkspace/
xcuserdata/
**/xcuserdata/
**/project.xcworkspace/xcuserdata/
DerivedData/
*.xcuserstate
build/

# macOS
.DS_Store

# Coverage / tooling
*.profdata
*.profraw
render-preview.png
