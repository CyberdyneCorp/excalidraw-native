{
  "name": "@xs/model",
  "version": "0.0.0",
  "description": "Element schema, scene, history, and .excalidraw/.excalidrawlib codecs — ported from ExcalidrawModel.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
